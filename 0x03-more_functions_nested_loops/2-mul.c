#include "holberton.h"
#include <stdio.h>

/**
 * mul - the sum two integers
 * @a: arg1 is a integer
 * @b: arg2 is a integer
 * Return: the multiplies of the input data.
 */
int mul(int a, int b)
{
	return (a * b);
}
